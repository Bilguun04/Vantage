authlib ~= 1.0
python-dotenv ~= 0.19
requests ~= 2.27
mongoengine ~= 0.27
pymongo ~= 4.0
# ===========================================
# Core Dependencies (Required for server)
# ===========================================

# Django and ASGI
django>=5.0,<6.1
channels>=4.0.0
daphne>=4.0.0

# Google Gemini SDK
google-genai>=0.4.0

# Environment variables
python-dotenv>=1.0.0

# ===========================================
# Mock Client Dependencies (Optional)
# ===========================================
# These are only needed to run mock_client.py

sounddevice>=0.4.6     # Audio capture/playback (no system deps needed)
numpy>=1.24.0          # Required for sounddevice
mss>=9.0.0             # Screen capture
pillow>=10.0.0         # Image processing
websockets>=12.0       # WebSocket client
